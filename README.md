# Install jar locally
```
mvn install:install-file \
   -Dfile=<path-to-file> \
   -DgroupId=io.corp \
   -DartifactId=calculator \
   -Dversion=1.0.0 \
   -Dpackaging=jar \
   -DgeneratePom=true

```
