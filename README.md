# Install jar locally
```
mvn install:install-file \
   -Dfile=<path-to-file> \
   -DgroupId=io.corp.calculator \
   -DartifactId=tracer \
   -Dversion=1.0.0 \
   -Dpackaging=jar \
   -DgeneratePom=true

```
